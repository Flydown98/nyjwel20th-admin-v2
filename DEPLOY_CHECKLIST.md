# v0.9.0 배포·리허설 체크리스트

## 배포 전
- 기존 운영 서버에서 JSON 백업 다운로드
- 현재 Google Drive 최근 성공 시각 확인
- Cloudtype 환경변수 `ADMIN_PASSWORD` 존재 확인
- `PUBLIC_BASE_URL`이 현재 운영 주소와 일치하는지 확인
- 문자나라 발신번호/계정 확인

## 배포 후
1. 로그인
2. 상단 버전 배지가 `최신 0.9.0`인지 확인
3. 접수대 이름 설정 (`접수대 1`, `배리어프리`, `VIP 데스크` 등)
4. 대시보드 인터넷/서버/문자/백업 상태 확인
5. 테스트 참가자 1명 QR 접수
6. 문자 도착 및 `/s/...` 좌석배치도 확인
7. 단체 일부만 도착 처리 → 미도착 멤버 예약좌석이 유지되는지 확인
8. 휠체어 포함 그룹 좌석 확인
9. 참가자 → 문자확인 → 실패 시 재발송 기능 확인
10. 행운권 진행자 화면과 `무대 추첨 화면`을 서로 다른 기기에서 열어 동기화 확인
11. Google Drive 수동 백업 1회
12. `행사 운영 잠금` ON 후 XLSX/복원/대규모 좌석배치가 잠기는지 확인

## 행사 시작 직전
- 모든 접수기기에서 화면/서버 버전 0.9.0 일치
- 모든 기기 접수대 이름 설정
- 운영 잠금 ON
- Google Drive 최근 성공 확인
- 휴대폰 핫스팟 비상망 준비
- 종이 또는 엑셀 비상명단 준비

## 행사 시작 후
- 단체 미도착 좌석은 자동 해제되지 않음
- 좌석을 풀 시점이 되면 메인 현황의 `미도착 예약좌석 해제`를 관리자 판단으로 사용

## v0.9.23 추가 확인
- 관리자 `/` 정상 접속 및 화면/서버 `0.9.23` 일치 확인
- 초대장 `/9-17` 조회 후 `초대장 이미지 저장` PNG 생성 확인
- 좌석안내 M~T 1~26 전체가 한 줄에 표시되는지 확인
- M~T 장애인/휠체어 우선석: 1·2 / 25·26 확인
- QR 접수에서 `테스트 모드 켜기` → QR 스캔 → 테스트 접수 성공 확인
- 테스트 모드에서는 참가자 도착상태, 기념품, 좌석, SMS 발송이 변경되지 않는지 확인


## v0.9.30 추가 변경
- v0.9.23 단체 좌석안내/동반그룹 기능 포함
- M~T 21~26번을 400석 적용 전에도 관리자 좌석판에서 직접 지정 가능
- 동반신청 그룹은 별도 제외목록을 사용하지 않음. `false`, `0`, `없음` 등은 실제 그룹코드가 아닌 미신청 값으로만 무시
- 기관 자동묶음 제외 기능은 유지


## v0.9.30 현장 확인
- [ ] 단체관리에서 기존 동반그룹과 동일기관 자동그룹이 모두 보이는지 확인
- [ ] 동반그룹 구성원 중 임의의 1명 QR 스캔 → 남은 동반자 전원 접수되는지 확인
- [ ] 동반그룹 접수 문자는 1건만 발송되는지 확인
- [ ] 문자 좌석안내 링크에서 동반자 좌석 전체와 이름별 좌석 목록이 보이는지 확인
- [ ] 테스트 모드에서 동일 흐름이 데이터 변경/SMS 없이 검증되는지 확인

## v0.9.30 추가 확인
- [ ] 현장배치 적용 후 휠체어 이용자가 G~N 1·2 / 29·30 끝자리 중심으로 배치되는지 확인
- [ ] 개인 QR 스캔만으로 자동접수되지 않고 `접수완료` 버튼을 눌러야 처리되는지 확인
- [ ] 접수 완료 QR 재스캔 시 그룹명/QR 제시자 정보가 보이는지 확인
- [ ] 좌석 변경 후 `좌석변경 문자 발송` 또는 즉시 발송 확인창이 정상 동작하는지 확인
