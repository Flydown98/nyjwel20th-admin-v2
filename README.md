# NYJWEL 20th Admin v2 — v0.8.0.0

이번 버전 핵심
- 개인 QR 접수 성공 후 팝업 자동 닫힘
- 중복 QR: 신규 처리/문자 재발송 없이 '이미 접수됨' 표시
- 내부 복지관/사회서비스/활동지원/이용인/팀 계열은 자동 기관 단체 후보에서 제외
- Cloudtype 서버에서 문자나라 API 직접발송
- EUC-KR 인코딩 적용
- 문자나라 테스트 발송 버튼
- 개별 수동 문자 발송
- 개인 QR 접수 완료 시 좌석 안내 문자 자동발송
- 단체 QR 접수 완료 시 대표자에게 이번 배정 좌석 목록 문자 자동발송
- 모바일 반응형 강화
- PWA(홈 화면 설치형) 기본 지원
- 기존 JSON 백업 복원 유지

Cloudtype 환경변수
- ADMIN_PASSWORD
- MUNJANARA_ID
- MUNJANARA_PW
- MUNJANARA_SENDER
- MUNJANARA_TEST_RECEIVER
- SMS_RELAY_TOKEN (이제 직접발송에는 필요 없지만 예전 relay 호환용으로 남겨도 됨)

적용 전
1. 현재 서버에서 JSON 백업 다운로드
2. ZIP 파일을 GitHub 저장소에 덮어쓰기
3. Cloudtype 재배포
4. 데이터가 비었으면 백업 메뉴에서 JSON 복원
5. 문자 메뉴에서 '테스트 문자 발송' 먼저 확인
6. 개인 QR 1명 테스트
7. 단체 QR 1팀 테스트

주의
- Cloudtype 직접발송이 성공하면 집 PC relay는 더 이상 필요하지 않습니다.
- 전날 안내문자는 자동 일괄발송 UI를 제거하고 개별 발송 중심으로 변경했습니다.


## v0.4.1 수정
- 문자 화면의 오래된 이벤트 핸들러 때문에 이후 JS가 중단되던 문제 수정
- JSON 백업 복원 버튼 정상 작동
- 문자 상태 확인/테스트 버튼 정상 작동
- 캐시 버전 분리로 구버전 app.js 혼합 방지


## v0.4.2 좌석맵 수정
- A~Y 25개 행
- 각 행 좌측 10석 + 중앙 런웨이 + 우측 10석
- 배정·미도착: 흰색 + 이름
- 도착완료: 초록색
- A~C 런웨이 안쪽 5석: 주요 귀빈(금색 테두리)
- A~C 바깥쪽 5석: 장애인·휠체어(파란 테두리)
- D~F: 내빈 관련 좌석(금색 계열 테두리)
- G~Y: 일반석


## v0.5 참가자 · 그룹 · 좌석관리
### 참가자
- 이름/연락처/기관/상태/도착/휠체어/장애인당사자/복지관이용/활성/메모 수정
- 참여/미참여/취소/비활성 상태 관리
- 접수/접수취소
- 관리자 수정 로그 기록

### 그룹
- 외부기관 자동 후보(내부 복지관/사회서비스 계열 제외)
- 기관 그룹 활성화
- 대표자 그룹 직접 생성
- 대표자 변경/구성원 수정
- 동반신청 companionGroup 자동 표시 및 관리
- 그룹 해제는 참가자 삭제가 아니라 연결만 해제

### 좌석
- 좌석 클릭 → 참가자 직접 지정
- 기존 좌석 참가자와 교환 또는 기존 참가자를 미배정 처리
- 좌석 해제
- 미배정 참가자 보기
- 미배정자 일괄배치
- 대표자/동반 그룹 연속좌석 우선
- 미도착 좌석 일괄 해제


## v0.6 데이터 관리
- 관리자 메뉴에 `데이터 관리` 추가
- XLSX/XLS 파일 선택
- 미리보기
- 참가자/좌석/설정/룰렛상품 요약
- 참가자/좌석 샘플 확인
- 중복 QR/연락처 공란 경고
- 최종 반영 전 자동 JSON 백업
- 최종 반영 후 자동 JSON 백업
- JSON 백업/복원 기능은 기존 백업 메뉴에 그대로 유지


## v0.7 현장 안정화
### 시네마틱 행운권 추첨
- 400명 전체를 마블룰렛에 렌더링하지 않음
- 전체 추첨 대상은 서버에서 유지
- 화면에는 샘플 이름을 빠르게 전환하는 두근두근 연출
- 3-2-1 카운트다운 후 최종 당첨자 공개
- 실제 당첨은 서버의 crypto 기반 랜덤 선택
- 도착완료 전체 / 복지관 이용인 / 장애인 당사자 / 휠체어 필터
- 중복 당첨 제외
- 당첨취소 시 다음 추첨 대상 복귀
- 경품 수령완료

### 실시간 동기화
- SSE(Server-Sent Events) 추가
- 한 접수대의 QR/좌석/그룹 변경이 다른 관리자 화면에 빠르게 반영
- QR 스캔 중/모달/추첨 연출 중에는 자동 화면갱신을 잠시 미룸

### 관리자 로그
- 별도 로그 메뉴
- 참가자 수정, 그룹, 좌석, 추첨, 수령 등 주요 작업 확인
- 접수로그 통합 표시


## v0.7.1 그룹 운영 규칙 수정
- 동반그룹은 대표자 개념 없이 어느 구성원의 QR을 찍어도 전체 그룹 접수창이 열림
- QR을 찍은 참가자를 이번 접수 대상에서 우선 처리
- 그룹 안내문자는 QR을 제시한 참가자의 연락처로 우선 발송
- 같은 외부기관 참가자가 2명 이상이면 자동으로 하나의 기관그룹으로 묶음
- 동반신청자도 같은 외부기관이면 별도 '동반코드' 이름 대신 기관명으로 운영
- 기관으로 묶이지 않는 동반신청 그룹 이름은 단순히 `동반`
- 기본 자동그룹 제외어에 `남양주시`, `남양주시장애인복지관`, 내부 팀/사회서비스/활동지원 관련 키워드 포함
- 그룹관리 화면에서 자동기관묶음 제외어를 직접 추가/삭제 가능
- 제외어 저장 후 자동그룹을 즉시 재구성 가능


# v0.8 기능 완성/안정화 패치

## 외부백업
- Cloudtype 로컬 JSON 상태는 그대로 메인 저장
- 1분 로컬 롤링백업 유지
- Google Drive JSON 외부백업 지원(Apps Script 브리지)
- Drive 최신 `latest.json`
- 지정 주기마다 시점 백업 생성
- Drive 백업 목록/특정 시점 복원
- 서버 시작 시 참가자 0명일 경우 Drive 최신백업 자동복원 옵션
- Drive 오류가 QR 접수를 막지 않는 비동기 구조

추가 환경변수:
- `GDRIVE_BACKUP_URL`
- `GDRIVE_BACKUP_TOKEN`

Apps Script 코드는 `google-drive-backup/Code.gs` 참고.

## 그룹
- 자동 기관그룹 직접 제외
- 제외된 기관/동반 목록 확인
- 제외 그룹 복원
- 같은 외부기관 자동묶음 유지
- 동반 중 누구의 QR이든 그룹 접수

## 좌석
- `G~Y 일반좌석 초기화`
- 참가자별 `좌석 고정`
- 참가자별 좌석 분류: 자동/일반, VIP, 내빈, 휠체어
- 최종 자동배치:
  VIP → 내빈 → 휠체어 → 대표자/동반/기관 연속좌석 → 일반 신청순
- A~F 특수구역 및 좌석고정 참가자 보호

## 문자
- 개별 문자
- 그룹 대표/연락가능 1명 문자
- 그룹 전체 문자
- 성공/실패 관리자 로그

## 설정
- 행사명/일시/장소/주최/정원
- QR 문자 ON/OFF
- QR 접수 즉시 좌석배정 ON/OFF
- 자동접수/팝업 시간
- 외부백업 ON/OFF/주기
- 빈 서버 자동복원 ON/OFF

## 역할
선택 환경변수:
- `RECEPTION_PASSWORD`
- `SEAT_PASSWORD`
- `RAFFLE_PASSWORD`

미설정 시 기존 `ADMIN_PASSWORD`만 사용.
설정 시 같은 로그인 화면에서 비밀번호에 따라 접수/좌석/추첨 메뉴가 제한됩니다.

## 백업
- 업로드한 JSON 복원 endpoint 보완
- 서버 임시디스크의 최근 로컬 백업 목록/선택 복원
- 기존 JSON 다운로드, CSV 다운로드 유지


# v0.8.1 · 777 스톱 행운권 추첨
- 기본 추첨대상: `도착 완료 + usesCenter(복지관 이용인) + 이전 미당첨자`
- 화면에는 `이름 (좌석번호)` 형식으로 빠르게 순환
- 추첨 준비 후 자동 회전, `SPACE`를 누를 때까지 계속 회전
- `SPACE` 입력 후 띠/띠/띠 4단계 감속
- 감속 시작 시 서버에서 crypto 기반 실제 당첨자 확정
- 최종 당첨자 + 좌석번호 대형 표시
- WebAudio로 회전 틱/감속음/당첨 팡파르 생성 (외부 음원 파일 불필요)
- 상품별 기존 유효 당첨건을 차감해 남은 수량 표시
- 상품 수량 소진 시 추가 추첨 차단
- 당첨취소 시 참가자는 다시 추첨대상이 되고 상품 남은 수량도 복구

※ 특정인을 숨겨서 일반 랜덤인 것처럼 보이게 당첨시키는 기능은 넣지 않았습니다.
  지정 대상이 필요하면 '별도 선정/보장 대상'으로 명확히 구분하는 방식으로 추가하는 것이 안전합니다.


# v0.8.2 · 행운권 상품/수량 관리
- 추첨 화면에서 상품명 + 총 수량 직접 등록
- 여러 상품 등록 후 드롭다운에서 이번 추첨 상품 선택
- 각 상품별 총수량 / 당첨수 / 남은수량 표시
- 상품명/수량 수정
- 상품 사용중지/재사용
- 당첨기록 없는 상품은 삭제 가능
- 이미 당첨기록이 있는 상품의 삭제 요청은 기록 보호를 위해 사용중지 처리
- 남은 수량보다 많은 인원을 추첨하려고 하면 서버에서 차단


# v0.8.3 · 버튼 동작 핫픽스
- 행운권 상품관리의 저장/중지/삭제 버튼을 명시적 `type=button`으로 변경
- 상품관리에서 `CSS.escape()` 의존 제거 (일부 브라우저 호환성 문제 방지)
- 주요 정적 버튼에 `type=button` 명시
- PWA 캐시 버전 강제 갱신
- 화면에서 JavaScript 오류가 발생하면 상단에 오류 문구가 보이도록 진단 배너 추가


# v0.8.4 · 버튼 치명 오류 수정 + 프리미엄 행운권 추첨
## 긴급 수정
- `app.js`에 잘못 남아 있던 단독 `async` 한 줄 제거.
- 이 오류 때문에 해당 줄 이후 JavaScript 이벤트가 등록되지 않아 여러 버튼이 동작하지 않던 문제 수정.
- 캐시 버전을 v0.8.4로 강제 갱신.

## 행운권 추첨 연출
- 3칸 슬롯 방식 제거.
- 한 개의 대형 세로 릴 방식으로 변경.
- 참가자 `이름 (좌석번호)`가 실제로 위아래로 연속 스크롤.
- SPACE를 누르기 전까지 계속 회전.
- SPACE 시점에 서버에서 실제 당첨자를 확정한 뒤 약 2.85초 동안 자연 감속하여 당첨자에 정확히 정지.
- 어두운 공연장용 프리미엄 배경: 은은한 스포트라이트, 앰비언트 글로우, 금빛 디테일, 유리질감 릴.
- 최종 당첨 시 전체화면 CONGRATULATIONS 연출 + 금빛 파티클.
- 기존 상품 등록/수량 관리, 복지관 이용인 필터, 이전 당첨자 제외, 당첨 취소/수령 처리 유지.


# v0.8.5 · PWA 설치 + 모바일 UI + 문자 좌석배치도
- 우측 상단 `앱 다운로드` 버튼 항상 노출. Chromium 설치 프롬프트 지원.
- iPhone/iPad는 설치 프롬프트가 없는 경우 `공유 → 홈 화면에 추가` 안내.
- 192/512 PNG PWA 아이콘 추가, manifest를 portrait-primary로 변경.
- 관리자 화면을 휴대폰 세로 화면에 맞춰 헤더/버튼/탭/통계/폼을 재배치.
- 공개 좌석 안내 페이지 `/seat-guide.html?k=...` 추가.
- 좌석 안내 링크는 HMAC 서명된 짧은 키를 사용하고, 이름/좌석 외 개인정보는 공개하지 않음.
- A~Y / 좌우 10석의 영화관 스타일 소형 좌석맵. 본인 좌석만 빨간색으로 강조.
- 개인 QR 접수 문자에 `좌석배치도:` 링크 자동 추가.
- 그룹 접수 문자에도 QR 제시/대표 연락 대상자의 좌석배치도 링크 추가.
- `PUBLIC_BASE_URL` 환경변수 지원. 미설정 시 현재 Cloudtype 공개 주소를 기본값으로 사용.


# v0.9.0 · 행사 운영 안정화판

이번 버전은 기능 추가보다 **행사 당일 사고 방지와 운영 가시성**을 우선합니다.

## 적용된 주요 개선
1. 단체 일부 도착 시 미도착자의 기존 예약좌석을 자동 해제하지 않음.
2. 행사 시작 후 필요할 때만 `미도착 예약좌석 해제` 버튼으로 일괄 해제.
3. 대시보드에 실제 입장(등록 도착 + 단체 초과), 최근 10분 입장, 미도착, VIP/내빈 미도착, 이동지원 미도착, 좌석 미배정, 문자 실패, 남은 좌석 표시.
4. html5-qrcode를 npm 의존성으로 포함하고 `/vendor/html5-qrcode.min.js`에서 자체 서비스하여 외부 CDN 의존 제거.
5. 서버/화면 버전이 다르면 상단에서 즉시 경고.
6. 공개 좌석배치도는 하드코딩 10×2가 아니라 실제 `state.seats`의 활성 좌석 구조를 읽어 그림. 본인 좌석만 빨간색 강조.
7. 휠체어 포함 그룹은 휠체어 이용인만 휠체어 가능 좌석을 먼저 사용하고 나머지 동반자는 일반 연속석을 우선 배정.
8. 참가자별 문자 발송 이력과 실패 문자 재발송 기능.
9. 좌석 안내 URL을 `/s/<서명키>` 형태로 단축.
10. `ADMIN_PASSWORD`가 없으면 운영판 서버가 시작되지 않도록 fail-closed.
11. `행사 운영 잠금` 모드 추가. 현장 접수/문자/추첨/수동 좌석은 유지하고 XLSX·복원·대규모 재배치 등 위험 작업은 잠금.
12. 기기별 `접수대 이름` 저장. 접수대1 / 배리어프리 / VIP 데스크 등으로 로그 구분 가능.
13. VIP/내빈·휠체어/이동지원 대상 도착 시 SSE로 모든 관리자 화면에 우선도착 알림.
14. 인터넷/서버/문자/백업 상태를 대시보드 상단에서 상시 확인.
15. 행운권 `진행자 화면`과 `무대 전체화면` 분리. 무대 화면은 별도 서명 링크로 열리고 추첨 시작/당첨 결과를 SSE로 동기화.
16. 별도 `SYSTEM_DEMO_MODE` 지원. 실제 SMS와 Drive 저장을 발생시키지 않고 전체 기능을 시연 가능.

## 운영판 필수 환경변수
- `ADMIN_PASSWORD`
- 기존 문자나라 환경변수
- 기존 Google Drive 백업 환경변수
- 필요 시 `PUBLIC_BASE_URL`

## 별도 시연 서버
같은 코드를 별도 Cloudtype 서비스에 배포하고 `SYSTEM_DEMO_MODE=true`로 설정하면 실제 데이터와 완전히 분리된 시연용 서버로 사용할 수 있습니다.
기본 시연 로그인 비밀번호는 `demo1234`이며 `DEMO_PASSWORD`로 변경할 수 있습니다.


# v0.9.1 · 통합 시연 화면 + 단체 운영 간소화
- 별도 시연 서버를 만들지 않고 운영 서버 안의 `/demo.html`에서 기능을 체험.
- 관리자 상단 `기능 시연` 버튼으로 새 창에서 열림.
- 시연 화면은 정적 샘플 데이터만 사용하며 실제 참가자, SMS, Google Drive, 좌석 상태를 건드리지 않음.
- 현황 / 개인 QR / 단체 접수 / 좌석 안내 / 문자 재발송 / 행운권을 간단히 체험 가능.
- 단체 접수 기본값은 남은 등록인원 전체. 먼저 온 사람이 전체 그룹을 한 번에 접수하는 운영 기준.
- 별도의 예약좌석 유지 로직과 `미도착 예약좌석 해제` 기능 제거.
- 일부만 접수하는 경우 아직 접수하지 않은 인원의 기존 좌석은 풀고 이후 실제 도착 때 다시 배정.


# v0.9.2 · 자동묶음 직접 수정
- 자동 기관그룹에도 `자동묶음 수정` 버튼 추가.
- 자동 동반그룹도 수정 가능.
- 자동그룹에서 구성원 추가/제거, 그룹명 변경, 대표자 변경 가능.
- 한 번 저장한 자동그룹은 `수정 고정` 상태로 전환되어 `자동그룹 다시 만들기`를 눌러도 덮어쓰지 않음.
- 필요 시 `자동으로 되돌리기`로 원래 기관/동반 자동묶음 규칙으로 복구.
- 다른 순수 자동그룹의 참가자를 수정 중인 그룹으로 옮길 수 있음.
- 수동 대표자 그룹 또는 이미 수정 고정된 다른 그룹에 들어간 참가자는 중복 편입을 차단.


# v0.9.3 · 좌석표/문자링크/참가자 현장 UI 개선
- v0.9.2 자동묶음 직접 수정 기능 포함.
- 내부 좌석 데이터는 기존 GL-01 / GR-01 구조와 호환을 유지하지만 모든 화면·문자 표시를 G1 ~ G20 형식으로 변경.
- 좌석관리 맨 위에 1~20 번호 헤더 추가. 10번/11번 사이를 중앙 통로로 시각 구분.
- 관리자 PC 좌석표를 대폭 압축하여 더 많은 행을 한눈에 볼 수 있게 개선.
- 관리자 휴대폰 좌석표는 이름을 숨기고 아주 작은 좌석 셀로 A~Y / 1~20 전체 배치를 한 화면 폭에 가깝게 표시.
- 참가자 목록을 번호 / 이름 / 기관 / 연락처 / 좌석 / 상태 / 관리 중심으로 축약.
- 문자 좌석표시도 `GL-01` → `G1`, `GR-10` → `G20`.
- 좌석안내 링크 키를 `p:451.e2702df3eca5` 형태에서 base36 접수번호 + 6자리 서명으로 단축.
- 기존에 이미 발송된 긴 좌석안내 링크도 계속 작동하도록 하위호환 유지.
- 공개 좌석안내 페이지도 1~20 헤더와 빨간 내 좌석 표시로 변경.


# v0.9.4 · 모바일 행운권 리모컨
- 컴퓨터에는 `raffle-stage.html` 무대 화면만 전체화면으로 띄우고, 휴대폰 관리자 페이지에서 조작 가능.
- 같은 Wi-Fi일 필요 없음. 휴대폰 5G / 컴퓨터 다른 인터넷이어도 Cloudtype 서버를 통해 동기화.
- 모바일 관리자에서 무대 화면 연결 대수 확인.
- `컴퓨터 무대화면 열기`, `무대 링크 복사` 지원.
- 무대 화면 전환: 타이틀 / 대기 / 검은 화면.
- 모바일에서 상품·대상·당첨인원 선택 → `추첨 시작`.
- 모바일 `멈춤 · 당첨자 공개`를 누르는 순간 서버에서 실제 당첨자를 확정하고 컴퓨터 무대 화면이 자연 감속 후 당첨자 공개.
- 새로고침/일시적인 네트워크 재연결 후에도 무대 화면이 현재 원격 추첨 상태를 서버에서 다시 동기화.
- `화면 초기화`로 다음 추첨을 위한 대기화면 복귀.
- 기존 PC 내장 추첨 기능도 그대로 유지.


# v0.9.5 · 행운권 무대화면 디자인 리뉴얼
- Lucky Draw 무대 화면을 행사장용으로 전면 리디자인했습니다.
- 단순 HTML 느낌을 줄이고, 커튼·스포트라이트·골드 포인트·유리 패널·입체형 당첨 카드로 구성했습니다.
- 캔버스 기반 부유 입자(ambient sparkle) 효과를 추가했습니다.
- 기존 모바일 원격제어/무대화면 연동 구조는 그대로 유지됩니다.
- 기능 변경 없이 시각 디자인만 강화한 버전이라 바로 배포 교체 가능합니다.


# v0.9.6 · 400석 ㄷ자형 좌석 + 순차 다중 행운권
- A~L: 좌 8석 + 런웨이 + 우 8석, 12줄 = 192석. A~F/G~L로 6줄씩 나뉜 좌우 총 4개 8×6 블록.
- M~T: 뒤쪽 26석×8줄 = 208석. 런웨이 끝을 감싸는 큰 뒤 블록. 총 400석.
- 좌석 클릭 1회는 선택 강조/이름·기관·상태 표시, 재클릭 또는 좌석 변경 버튼은 편집.
- 여러 명 행운권은 한 명씩 룰렛 정지/발표, 마지막에는 전체 당첨자를 중앙에 표시.


# v0.9.7 · 뒤쪽 블록 20석으로 축소
- A~L 앞 12줄: 좌 8석 + 런웨이 + 우 8석 = 192석 유지.
- M~T 뒤 8줄: 한 줄 20석 × 8줄 = 160석으로 변경.
- 현재 총 좌석수는 352석.
- 뒤쪽 좌석을 20열 기준으로 관리자/모바일 좌석배치도에 맞게 축소.
- 행운권 순차추첨 기능은 v0.9.6 그대로 유지.


# v0.9.8 · 뒤쪽 좌석 선택 수정 + 사전 좌석확정 보호
- M~T 뒤쪽 좌석은 서버가 반환한 실제 좌석 데이터를 기준으로 렌더링하여 1~20석 모두 선택 가능.
- 좌석 셀/이름 영역 어디를 눌러도 좌석 선택이 되도록 클릭 이벤트 안정화.
- 서버의 뒤쪽 좌석 생성 자체도 실제 20석으로 수정(이전 버전에 남아 있던 26석 생성 코드 제거).
- `현재 배정좌석 확정` 버튼 추가.
- 9월 16일까지 미리 좌석이 배정된 참가자들을 한 번에 `seatLocked=true` 처리 가능.
- 좌석확정 참가자는 행사 당일 개인 QR 접수 시 기존 좌석을 유지.
- 단체 일부 접수에서도 좌석확정 참가자의 좌석은 해제되지 않음.
- 접수취소를 하더라도 사전 확정좌석은 유지.
- 관리자가 좌석관리에서 직접 지정/교환하는 작업은 계속 가능.
- 선택한 좌석에 `좌석확정` 배지가 표시되며 필요하면 개별적으로 확정 해제 가능.


# v0.9.10 · 행사장 웹 슬롯머신 무대
- EXE 없이 행사장 PC에서 기존 `raffle-stage.html?k=...` 웹페이지를 열어 사용합니다.
- 처음 `무대 화면 시작` 버튼을 한 번 누르면 전체화면과 효과음을 활성화합니다.
- 이후 휴대폰 관리자 페이지의 무대 원격제어로 시작/멈춤/다음/대기/검은화면을 조작합니다.
- 21:9 3440×1440을 우선 디자인하되 FHD/16:9에서도 비율에 맞게 반응형으로 동작합니다.
- 룰렛 느낌을 줄이고 3칸이 보이는 단일 슬롯머신으로 변경했습니다.
- 이름/좌석은 서로 다른 컬럼을 사용하고 말줄임/동적 글자 크기를 적용해 글자 겹침을 방지했습니다.
- 네이티브 프로토타입에서 효과음(`stage-roulette.wav`, `stage-winner.wav`)과 배경/프레임 계열 디자인을 차용했습니다.
- 복수 추첨은 한 명씩 공개하고 마지막에는 FINAL WINNERS 카드 화면을 표시합니다.
- Google Apps Script 파일은 이 GitHub 배포 패키지에 포함하지 않습니다. 기존 외부백업 설정은 그대로 사용합니다.


# v0.9.11
- 마지막 당첨자도 슬롯 감속 → 중앙 고정 → 당첨자 단독 화면 → FINAL WINNERS 순서로 표시.
- 서버는 마지막 당첨자 이벤트 후 9초 뒤 최종 명단을 전송.
- 뒤쪽 좌석 M~T는 왼쪽부터 1~20번, 정확히 8줄.
- `352석 최신 배치 적용`은 기존 호환 좌석을 최대한 유지하며 이전 좌석 데이터를 변환.
- 모든 좌석 수동 지정 가능.
- 자동좌석 기능 ON. 일반/VIP/내빈/휠체어 구역 규칙은 유지.


# v0.9.12
- 참가자 목록에서 참가자 삭제 가능. 기존 문자/행운권 감사로그는 유지.
- 좌석관리 빈 좌석에서 이름/연락처/기관만 입력해 새 참가자를 즉시 좌석에 추가 가능. 기본 좌석확정 처리.
- 최근 당첨 로그에 `당첨확인문자 보내기` 추가.
  - 단체 참가자는 대표 연락처 우선, 없으면 연락 가능한 그룹원/당첨자 번호 사용.
  - 문자에 당첨자/경품/좌석/수령 안내 포함.
  - 대표자가 문자 확인 후 대신 수령 가능하도록 안내.
- 행운권은 1~5명 동시 추첨.
  - 1명도 가능.
  - 2~5명 선택 시 그 수만큼 슬롯이 동시에 회전.
  - 멈춤 시 슬롯이 왼쪽부터 빠르게 순차 정지.
  - 모두 정지한 뒤 전체 당첨자 화면으로 전환.
- Cloudtype 환경변수 추가 없음.
- Google Apps Script 변경 없음.


# v0.9.13 · 행사 최종 안정화
- 행운권 무대 애니메이션을 transform 기반 긴 릴에서 **독립 슬롯 카드 방식**으로 재작성.
- 1~5명 동시 추첨. 각 슬롯은 이름을 빠르게 교체한 뒤 감속 간격이 길어지며 순서대로 당첨자에 고정.
- 5명 추첨도 전체 연출이 과하게 길어지지 않도록 각 슬롯 감속을 겹쳐 진행.
- 상품 잔여수량에 따라 추첨인원 1~5명을 자동 추천.
- 사전 좌석배치 정책을 다음 3가지로 제한:
  1. 내빈(vip/guest)
  2. 휠체어/장애인석
  3. 관리자가 직접 지정하고 확정(seatLocked)한 참석확정 인원
- 일반 참가자는 사전배치하지 않고 행사 당일 체크인 시 자동 좌석배정.
- 좌석 문자 링크 화면을 현재 A~T / 앞 16석 / 뒤 20석 구조에 맞게 완전히 재작성.


# v0.9.14
- 사용자 안내에서 좌석이 없는 참가자는 `미배정`이 아니라 `스탠딩석`으로 통일.
- 접수 완료 SMS, 참가자 목록, 행운권 로그, 좌석 안내 링크에서도 스탠딩석 표기.
- 행운권 무대에서 참가자를 `이름 (휴대폰 끝 4자리)`로 표시.
  예: `홍길동 (1234)`.
- 전화번호가 없는 참가자는 이름만 표시.
- 행운권 기록에도 전화번호 끝 4자리/표시명을 저장해 추후 기록 확인 가능.


# v0.9.15
- A~C 우선석의 관리자 화면 색상 분류 오류 수정.
- 서버 좌석분류와 화면 좌석분류를 동일하게 통일.
- A~C 왼쪽 1~4 / 오른쪽 13~16 = 휠체어·장애인석.
- A~C 왼쪽 5~8 / 오른쪽 9~12 = VIP.


# v0.9.16 · 좌석배치 변경
- A~F: 기존 16석 + 런웨이 구조 유지, 장애인석 없이 전부 내빈석 표시.
- G~T: 한 줄 20석 x 14줄. 1·2번, 19·20번을 장애인·휠체어 우선석으로 표시.
- 휠체어 신청자 그룹: 휠체어 1명 + 보호자 1명을 양끝 우선석 한 쌍에 배치. 추가 동반자는 같은 줄 안쪽 일반석에 우선 배치.
- `휠체어 신청자만 재배치` 버튼 추가. 다른 참가자 좌석은 유지.
- 기존 G~L의 16석 좌석은 새 G~L 20석 구조의 같은 표시번호로 최대한 유지.
- 총 좌석 376석.


# v0.9.20 · 좌석 화면/서버 버전 동기화
- 관리자 HTML, JS, CSS 캐시키, 서비스워커, 서버 API, package 버전을 0.9.20로 통일.
- 서비스워커 등록 자체를 캐시 우회(updateViaCache:none)하고 즉시 update 요청.
- 정적 파일 Cache-Control을 no-store로 강화.
- 현재 좌석 구조를 A~F 런웨이 16석 / G~T 20석, 총 376석으로 명확히 표기.
- 예전 A~L 런웨이/400석 문구가 캐시로 섞여 보이는 문제 방지.


# v0.9.20 · 400석 좌석배치 복원
- 2026-09-16 JSON/CSV의 기존 좌석코드를 기준으로 A~L 배정좌석을 그대로 유지합니다.
- A~L: 좌8 + 런웨이 + 우8, 12줄 = 192석.
- M~T: 기존 MB~TB 01~20을 그대로 유지하고 21~26을 추가, 26석 × 8줄 = 208석.
- 총 400석. 기존 배정자는 좌석코드가 변경되지 않습니다.


## v0.9.20 경로 및 CSV 병합
- 관리자: `/` 또는 `/admin`
- 공개 초대장 조회: `/9-17`
- 참가자 CSV 다운로드 후 새 행을 추가하여 `참가자 CSV 병합 가져오기`로 다시 올릴 수 있습니다.
- 기존 QR고유코드가 있는 행은 해당 참가자를 갱신하며, QR고유코드가 비어 있는 새 행은 접수번호와 QR고유코드를 자동 생성합니다.
- CSV 병합은 기존 참가자를 삭제하지 않으며 반영 전/후 JSON 백업을 자동 생성합니다.
